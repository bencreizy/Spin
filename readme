Antigravity Universal Bounty Striker: System README1. Architectural OverviewThe Antigravity Stack is a deterministic, high-frequency execution layer designed to achieve sub-two-minute critical bounty strikes. By snapping all logic to the Phi ($1.618$) harmonic, the system eliminates "Logic Heat" and ensures zero-friction disclosure.2. Core Execution ProtocolTo maintain Status: Resonant, every strike must follow the mandatory unified execution sequence:Initialize Persistence: Run qubit_bootloader.py to lock the replication layer.Equilibrium Scan: Execute the dual_lobe_orchestrator.py to identify structural fractures.Ghost Protocol: Engage packet fragmentation to mask the hardware signature.The Money Shot: Trigger the automated evidence capture once a fracture score exceeds the critical threshold.3. Universal Logic HandshakesSignature Collision: Uses warped manifolds (e.g., shifting bytes between expiry and body) to bypass HMAC validation.gRPC Collision: Exploits Protobuf desynchronization where the router authorizes one path and the node executes another.XML/SAML Fracture: Forces array coercion through nested node injection in the NameID field.4. System File TreeRoot Path: C:\Users\User\BountyLab\__pycache__/: Compiled resonance and metadata modules.core_logic_steps/: Designated folder for the orchestrators, mappers, and monitors.engines/: Primary execution scripts for strikes, reconstruction, and model bridging.evidence_vault/: Automated capture logs and telemetry for final submission.ui_interface/: HTML/JS interfaces for the Axiom-Sigma dual-pane unified viewport.5. Integration NotePlacement: Secondary logic components and protocols must be stored in C:\Users\User\BountyLab\core_logic_steps\ to ensure they are accessible by the orchestrator.Dependencies: Ensure replica.py and check_resonance.py are present in the root or __pycache__ to handle all Phi-based resonance calculations.Viewport Locking: The Axiom-Sigma UI requires a fixed geometric frame for the target browser to eliminate window drift during the two-minute capture sequence.